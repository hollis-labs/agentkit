package agentruntime

const (
	// ModulePath is the canonical import module for this library.
	ModulePath = "github.com/hollis-labs/agentkit/agentruntime"

	// Scope summarizes the root package's role. Implementation APIs live in
	// focused subpackages such as runtimebind, turn, bootdir, and sessionkit.
	Scope = "shared agent runtime substrate"
)
